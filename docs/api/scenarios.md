# Scenarios

A `Scenario` bundles a baseline parameter set with a list of interventions. `run_scenario` executes the baseline vs intervention comparison, and `compare_scenarios` aggregates results across multiple scenario runs.

::: scenarios
    options:
      members:
        - Scenario
        - run_scenario
        - compare_scenarios
